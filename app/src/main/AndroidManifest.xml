<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.DECarpooling"
        tools:targetApi="31">
        <activity
            android:name=".NavigatoinsBottom"
            android:exported="false"
            android:label="@string/title_activity_navigatoins_bottom" />
        <!-- <activity -->
        <!-- android:name=".Nav" -->
        <!-- android:exported="false" -->
        <!-- android:label="@string/title_activity_nav" /> -->
        <!--
             TODO: Before you run your application, you need a Google Maps API key.

             To get one, follow the directions here:

                https://developers.google.com/maps/documentation/android-sdk/get-api-key

             Once you have your API key (it starts with "AIza"), define a new property in your
             project's local.properties file (e.g. MAPS_API_KEY=Aiza...), and replace the
             "YOUR_API_KEY" string in this file with "${MAPS_API_KEY}".
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="YOUR_API_KEY" />

        <activity
            android:name=".Mapselectplace"
            android:exported="false"
            android:label="Mapselectplace" />
        <activity
            android:name=".getride"
            android:exported="false" />
        <activity
            android:name=".Offerride"
            android:exported="false"
            android:label="offerride"
            android:theme="@style/Theme.DECarpooling" />
        <activity
            android:name=".choosecar"
            android:exported="false" /> <!-- <activity -->
        <!-- android:name=".MainActivity" -->
        <!-- android:exported="false" /> -->
        <activity
            android:name=".MapsActivity"
            android:exported="false"
            android:label="maps" />
        <activity
            android:name=".MainPage"
            android:exported="false" />
        <activity
            android:name=".WelcomePage"
            android:exported="false" />
        <activity
            android:name=".LoginActivity"
            android:exported="false" />
        <activity
            android:name=".signinactivity"
            android:exported="false" />
        <activity
            android:name=".Firstactivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ContactUsActivity"
            android:exported="false" />
    </application>

</manifest>